	// create the controller and inject Angular's $scope
angular.module('DigitalMarket').controller('trafficController', function($scope) {
		// create a message to display in our view
		$scope.message = 'traffic controller!';
	});